# Application
spring.application.name=MyTogether
server.port=8080

# PostgreSQL Database Configuration (Supabase Cloud - Active for Dev)
spring.datasource.url=jdbc:postgresql://aws-1-ap-southeast-1.pooler.supabase.com:6543/postgres?prepareThreshold=0
spring.datasource.username=postgres.fldmuhxmtvlcurjllyfl
spring.datasource.password=zXYwikvfbNetGvap
spring.datasource.driver-class-name=org.postgresql.Driver

# PostgreSQL Database Configuration (Docker - Local)
#spring.datasource.url=jdbc:postgresql://localhost:5432/mytogether
#spring.datasource.username=mytogetheruser
#spring.datasource.password=mytogetherpass
#spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# JWT Configuration
jwt.secret=kX7vP9mZ2nQ8rT4wY6uB3cH5jL0aF8eD1gK9sM2vN4x=
jwt.expiration=86400000

# Async Configuration
spring.task.execution.pool.core-size=5
spring.task.execution.pool.max-size=10
spring.task.execution.pool.queue-capacity=100

# Response Compression (70% smaller responses)
server.compression.enabled=true
server.compression.mime-types=application/json,application/xml,text/html,text/xml,text/plain
server.compression.min-response-size=1024

# Swagger Configuration
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.enabled=true

# Logging
logging.level.org.springframework.security=DEBUG
logging.level.com.google.maps=DEBUG

# Google Maps API
google.maps.api.key=AIzaSyAxJ0IUK019PrkXtqKAiN8wWq9C7R056d4

# Jackson JSON
spring.jackson.serialization.indent_output=true

# Disable Spring Security completely
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.security.servlet.SecurityAutoConfiguration

# Supabase Storage (for images) - Default values for local development
supabase.url=https://fldmuhxmtvlcurjllyfl.supabase.co
supabase.key=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImZsZG11aHhtdHZsY3VyamxseWZsIiwicm9sZSI6InNlcnZpY2Vfcm9sZSIsImlhdCI6MTc2NDk5MjAyMCwiZXhwIjoyMDgwNTY4MDIwfQ.lrWP-NWWFhUxRDhYUk-yu7Rm582kJmHcFMvvAQNIe1k
supabase.bucket=shop-photos

# ========================================
# PRODUCTION CONFIGURATION (Railway + Supabase)
# ========================================
spring.config.activate.on-profile=prod

# Server port (Railway sets this typically, but default to 8080)
server.port=${PORT:8080}

# Database (Supabase PostgreSQL via ENV VARS)
spring.datasource.url=${SUPABASE_DB_URL}?prepareThreshold=0
spring.datasource.username=${SUPABASE_DB_USER}
spring.datasource.password=${SUPABASE_DB_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate (Prod settings)
spring.jpa.show-sql=false
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.properties.hibernate.format_sql=false

# Supabase Storage (via ENV VARS)
supabase.url=${SUPABASE_URL}
supabase.key=${SUPABASE_KEY}
supabase.bucket=${SUPABASE_BUCKET:shop-photos}

# Logging (Quieter in Prod)
logging.level.org.springframework.security=INFO
logging.level.com.google.maps=INFO
logging.level.org.hibernate=WARN

# Better Stack (Logtail) Logging
logtail.source-token=${LOGTAIL_SOURCE_TOKEN:}
